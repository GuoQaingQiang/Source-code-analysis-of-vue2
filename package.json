{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "lerna run clean --stream",
    "start:mustache": "lerna run --include-filtered-dependencies --scope mustache start --parallel"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
